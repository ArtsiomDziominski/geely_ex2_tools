# AOSP platform test keys (public).
# Same certificate as CentralEXAuto-geely-platform-signed.apk
# (EMAILADDRESS=android@android.com, SHA-256 c8a2e9bc…).
#
# Used by scripts/build-system-apk.ps1 to produce a system-UID APK
# that can call CarAudioManager.setAVASMode (CAR_CONTROL_AUDIO_VOLUME).
#
# Source: platform/build target/product/security (platform.pk8 / platform.x509.pem)
